from setuptools import setup, find_packages

setup(
    name='receiver',
    version='0.1.0',
    py_modules=['receiver.receiver'],
    install_requires=[
        # 'pyngrok',
        # 'Flask',
    ],
)
